# RunescapeEquipment
import runescape item stats

1. pulls item info from the runescape wiki using "import from wiki"
2. this info is added to the "items stored" jsonl file
3. running "equipped items" opens a gui where you can select between items in each item slot category and you can see the total of each stat, similar to in game
